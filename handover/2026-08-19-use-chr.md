# Handover — architect session "use chr" (income-portfolio / PrintProfit), 18–19 Aug 2026

Role: architect, working directly from the owner (not on the supervisor's plan). Project:
`income-portfolio` (PrintProfit). Shift ran from "time to get sales, £20 ad budget" to shift end
~00:30 on 19 Aug. Everything below is committed and pushed to `riglerkarve/profitprint` (last
commit 8596ba7 + the verification files); nothing is staged in either repo.

## Built
- `marketing/PAID-TEST.md` — the Microsoft Ads search test plan (arithmetic, keywords, ads,
  kill criteria) and a running status section; `CLAUDE.md` guardrail amended to record the
  one-off ad budget (planned £20, funded £40 by the owner at the PayPal screen).
- Microsoft Ads account `PrintProfit (G120TVM4)`, UK, GBP, PayPal prepay; **two live Search
  campaigns** (A: Max Clicks, cap £0.50; B: Enhanced CPC £0.40; £2/day each), 13 phrase/exact
  keywords, campaign negatives, RSA ads, sitelinks; landing `/tool/?utm_source=bing…`.
- Calculator (`tools/print-cost-calculator/index.html`): Cloudflare beacon added (it was only in
  the Astro layout — tool visits before 18 Aug 21:50 are *unmeasured*, not zero), source-aware
  coupon display (SEARCH20/REDDIT20/GROUPS20).
- Product: spreadsheet v2.0 (seed-row fix 8→0.08, Etsy preset aligned, `_xlfn.MINIFS`, P&L over
  40 rows, premium Start Here/Settings, editions), cover + gallery + preview PDF on Payhip,
  three Payhip coupons; owner signed off after opening it in LibreOffice.
- `scripts/price-link.cjs` (calculator maths on the CLI + pre-filled `#hash` link),
  `marketing/REPLY-KIT-2026-08.md` (7 verified live threads, replies around the asker's numbers;
  **#1 and #2 posted from the owner's Reddit account 18 Aug ~23:20/23:35**),
  `marketing/LAUNCH-KIT.md` + patch for an awesome-3d-printing PR (held), Etsy listing pack +
  4 images (no shop opened).
- Search Console: two HTML-file verification tokens deployed live at `/profitprint/google*.html`.

## Verified
- Live calculator renders the pre-filled link exactly ($8.25 → $21.48 → 50%); every kit link
  checked against the calculator's 16 SHARE_FIELDS; beacon confirmed on the served `/tool/`.
- Spreadsheet v2.0 checked against the *delivered* CI artefact: `check-delivered-xlsx.cjs` 0
  errors, LibreOffice one-page-per-sheet PDF read by eye; Payhip public page shows v2.0
  (16 KB), cover, gallery, preview — checked by `curl`, not in the editor.
- Microsoft Ads: both campaigns listed, "in review"/"learning", balance banner gone after
  funding. Reddit: both comments visible on the threads.
- **Cloudflare Web Analytics: first real data — 3 visits / 3 page views in 24 h, all `/tool/`.**
- Verification files answer 200 with the right body.

## Deviations
- Budget: owner funded £40 (£33.33 net of VAT) against the planned £20 — recorded in
  PAID-TEST.md and CLAUDE.md; re-based to ~16 days at £2/day. Not a second top-up.
- Locations: default bundle (US, UK, CA, IE) kept; plan said AU instead of IE.
- "No Manual CPC" exists in the new editor; ran Enhanced CPC + Max-Clicks-with-cap as an A/B.
- Another session's uncommitted edits to the calculator/dashboard/astro config were reviewed,
  kept, and named in my commit rather than reverted.
- Did not add UET/conversion tracking (tracker; privacy page says none) — by design.

## Risks
- Ads can be rejected in editorial review (landing page is a github.io subdomain); check ~20 Aug.
- At £2/day Bing volume may be slow; a zero on day 2 is "not served yet", not "no interest".
- The ad-blocker/extension interaction that blanked the Microsoft Ads editor will recur on
  any Microsoft surface — `chrome-extension-gotchas` memory has the fix.
- `data/metrics.json` still lists Cloudflare as "pending-setup" and nothing reads analytics
  automatically; the daily briefing will keep printing 0 visits until `scripts/cf-analytics.mjs`
  exists (not started). That is a stale-zero risk in the report, not a data loss.
- Search Console property is owned by a third Google account; until the owner verifies under
  one in Chrome, no session can submit the sitemap.
- Live surfaces: nothing half-done (no open drafts in Reddit, Payhip, Microsoft Ads, Cloudflare).

## Next
- 20 Aug: read Microsoft Ads (clicks, CPC, spend per campaign, review status), Cloudflare
  `/tool/` by referrer (bing vs reddit), Payhip views + SEARCH20/REDDIT20 uses vs baseline
  (3 views / 0 orders, 18 Aug). Apply PAID-TEST.md §6 kill criteria as written.
- Build `scripts/cf-analytics.mjs` (Cloudflare GraphQL, account 9d7e2f9ebb96ee3b3cee3b1b80b37156,
  site tag 22c0da83544c4442b3cb06a4cadabc12) → `data/metrics.json` + `data/analytics.json`,
  wired into `daily-briefing.yml` with absence vs could-not-look kept distinct; flip the
  Cloudflare source status to live.
- Reply kit #3 and #4 next (one or two a day), via clipboard + Markdown mode; owner posts.
- Submit the sitemap once Search Console is verified under a Chrome-signed-in account.

## Blocked on you
- Click **VERIFY** in the Search Console dialog (files are live for both Google accounts); then
  the sitemap can be submitted.
- Create a read-only Cloudflare API token (My Profile → API Tokens → "Read analytics and logs",
  scoped to the account) and store it: `gh secret set CF_API_TOKEN --repo riglerkarve/profitprint`
  — the token should not pass through me or the repo.
- One word on the awesome-3d-printing PR ("open it") — fork + PR appear under your GitHub.
- Etsy shop: the pack is ready; the set-up fee decision at signup is yours.
