# Codex Worker — Batch I (M111–M113)

## Built

- Added `tools/verify-suite.cjs`: one entry point for the verification suite. It discovers checker-like tools, runs only an explicit safe subset, and lists every manual or unclassified tool as residue instead of treating it as a pass.
- Added `baselines/verification-suite.json`, the reviewed contract of 23 check candidates: 9 safe automatic checks and 14 manual checks with their reasons.
- Added `reference/verification-suite-unattended.md`, a no-schedule proposal for later unattended use.

## Verified

- `node --check tools/verify-suite.cjs` exited 0.
- `node tools/verify-suite.cjs` completed with `9 safe pass; 0 safe fail; 14 manual; 0 unclassified; baseline matches.`
- The automatic set includes only source checks, read-only live-database checks, and verifiers that create a unique temporary database. `migrate-from-zero`, `schema-integrity --self-test`, `verify-restart-lock`, and `verify-route-failures` each named and removed their temporary paths. No automatic test opened `data/dashboard.db` writable.
- `node tools/verify-suite.cjs --write-baseline` correctly refused without `--confirm-write`.

## Blocked

- None.

## Deviations

- `MANUAL` is intentionally not a passing result: 14 checks remain excluded because they write live state, use external services, mutate the shared tree to plant a defect, or depend on current in-progress artefacts.

## Blocked on you

## Next

- Batch J (M114–M115): independently review the architect's 19 August commits, then review this week's own changes under the cross-engine rules.
